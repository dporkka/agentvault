# Main CI stabilization backport

This branch backports the CI/CD stabilization validated in PR #57 onto `main` without merging either independent AgentVault feature stack.

The marker also ensures the backport receives a normal Contents-API commit before merge validation.
